podTemplate (
    containers: [
        containerTemplate(name: 'nodejs', image: "node:12-alpine", ttyEnabled: true, command: 'cat')
    ]
) {
    node(POD_LABEL) {
        stage('Run shell') {
            container('nodejs') {
                sh 'echo hello world'
            }
        }
    }
}